load("@fbcode_macros//build_defs:cpp_binary.bzl", "cpp_binary")
load("@fbcode_macros//build_defs:cpp_unittest.bzl", "cpp_unittest")

cpp_binary(
    name = "align_cpp",
    srcs = [
        "Align.cpp",
    ],
    headers = [
        "alignment/Utils.h",
    ],
    deps = [
        "//deeplearning/projects/wav2letter/src/common:common",
        "//deeplearning/projects/wav2letter/src/criterion:criterion",
        "//deeplearning/projects/wav2letter/src/data:data",
        "//deeplearning/projects/wav2letter/src/fb:everstore_data",
        "//deeplearning/projects/wav2letter/src/runtime:runtime",
    ],
    external_deps = [
        "gflags",
        "glog",
        ("Cereal", None, "cereal"),
    ],
)

cpp_unittest(
    name = "align_test",
    srcs = [
        "AlignTest.cpp",
    ],
    headers = [
        "alignment/Utils.h",
    ],
    tags = ["serialize"],  # run GPU tests sequentially to avoid OOMs
    deps = [
        "//deeplearning/projects/wav2letter/src/common:common",
    ],
    external_deps = [
        "gflags",
        "glog",
        ("Cereal", None, "cereal"),
    ],
)
